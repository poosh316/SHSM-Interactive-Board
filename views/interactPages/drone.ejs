<!DOCTYPE = html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>drone</title>

	<link href="/styles.css" rel="stylesheet" />
</head>

<body>
    <div class="navBar">
        <ul>
            <li><a href="/home"><img id="logo" src="/images/SHSMLogo.png" alt="SHSM" /></a></li>
            <li><a href="/home">Home</a></li>
            <li><a href="/about">About</a></li>
            <li class="dropdown">
                <a class="current" href="javascript:void(0)" class="dropbtn">Interact</a>
                <div class="dropdownList">
                    <a href="/int/lights">Lights</a>
                    <a href="/int/drone">Drone</a>
                </div>
            </li>
            <li><a id="signup" href="/signup">Sign Up</a></li>
        </ul>
    </div>

    <br>

    <div class="disclaimer">
        <h4>
        <div>
            Session expires in: <span id="time-remaining" data-seconds="<%= secondsRemaining %>">
                Loading...
            </span>
        </div>
        </h4>
    </div>

    <img id="drone" src="/images/droneOff.png" alt="drone" />
    <br>
    <br>
    <button id="droneBtn">Click to run the drone for 10 seconds</button>


    <div id="auth-status-container">
        <% if (isAuthenticated) { %>
            <!-- Show this text if the cookie exists and JWT is valid -->
            <p>Welcome back, user!: Logout</p>
        <% } else { %>
            <!-- Show this text if there is no valid cookie -->
            <p>Hello, guest!: Login</p>
        <% } %>
    </div>
    <!--you will also need to change it bellow in the script where it says
    document.getElementById('auth-status-container').innerHTML-->
    

	<script src="/script.js"></script>
    <script src="/interact.js"></script>
    <script>
        const timeSpan = document.getElementById('time-remaining');
  
        if (timeSpan) {
            let timeLeft = parseInt(timeSpan.getAttribute('data-seconds'), 10);

            const interval = setInterval(() => {
            if (timeLeft <= 0) {
                clearInterval(interval);
                timeSpan.textContent = "Session Expired";

                // Update the HTML content immediately without reloading
                //i recommend making this say the same thing as the else above 
                    // otherwise when you refresh the page it will appear as else 
                    // momentarily than this if a session has been created and expired
                document.getElementById('auth-status-container').innerHTML = 
                    `<p>Your session has expired.: Click here to Login again.</p>`;
                
                return;
            }

            // Calculate minutes and seconds
            const minutes = Math.floor(timeLeft / 60);
            const seconds = timeLeft % 60;
            
            // Display formatted time (e.g., 14:05)
            timeSpan.textContent = `${minutes}:${seconds.toString().padStart(2, '0')}`;
            
            timeLeft--;
            }, 1000);
        }
    </script>
</body>
</html>